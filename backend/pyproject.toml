[project]
name = "studyplaner-api"
version = "0.1.0"
description = "Cloudflare Worker API and D1 import tooling for StudyPlaner."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
